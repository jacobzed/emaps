{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=census2;Username=postgres;Password=password"
  },
  "Secrets": {
    // This is exposed publicly, make sure the key only has access to the Javascript Map API
    // and is locked to a specific domain name. https://console.cloud.google.com/
    "MapsApiKey": "your-key"
  }
}
